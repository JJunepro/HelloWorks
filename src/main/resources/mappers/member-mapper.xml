<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="Member">
<resultMap type="Member" id="MemberMap">
		<id property="uid" column="U_ID" />
		<result property="oCode" column="O_CODE" />
		<result property="pwd" column="PWD" />
		<result property="dept" column="DEPT" />
		<result property="name" column="NAME" />
		<result property="birth" column="BIRTH" />
		<result property="resident" column="RESIDENT" />
		<result property="address" column="ADDRESS" />
		<result property="mail" column="MAIL" />
		<result property="phone" column="PHONE" />
		<result property="gender" column="GENDER" />
		<result property="cPhone" column="C_PHONE" />
		<result property="salary" column="SALARY" />
		<result property="pImage" column="P_IMAGE" />
		<result property="salaryDate" column="SALARYDATE" />
		<result property="entry" column="ENTRY" />
		<result property="last" column="LAST" />
	</resultMap>
	  <insert id="memberAdd"  parameterType="Member">
       INSERT INTO MEMBER(NAME,U_ID,PWD,O_CODE,DEPT,BIRTH,RESIDENT,ADDRESS,MAIL,GENDER,PHONE,C_PHONE,SALARY,SALARYDATE,P_IMAGE,ENTRY)
        VALUES (#{name},#{uid},#{pwd},#{oCode},#{dept},#{birth},#{resident},#{address},#{mail},#{gender},#{phone},#{cPhone},#{salary},#{salaryDate},#{pImage},sysdate)
    </insert>
    
 <select id="login" resultMap="MemberMap" parameterType="Member">
        SELECT * FROM MEMBER
        WHERE U_ID = #{uid} AND PWD = #{pwd}
    </select>
     <select id="idSearch" resultMap="MemberMap" parameterType="Member">
        SELECT NAME, U_ID FROM MEMBER
        WHERE NAME = #{name} AND MAIL = #{mail} 
    </select>
     <select id="pwdSearch" resultMap="MemberMap" parameterType="Member">
        SELECT NAME, PWD FROM MEMBER
        WHERE U_ID = #{uid} AND NAME = #{name} AND MAIL = #{mail}
    </select>
    <select id="profile" resultMap="MemberMap" parameterType="Member">
        SELECT * FROM MEMBER
       	WHERE U_ID = #{uid}
    </select>
       <select id="memberAll" resultMap="MemberMap" parameterType="Member">
        SELECT * FROM MEMBER
    </select>
    
   
</mapper>