<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="AttendanceDAO">

<!--
<sql id=""></sql>
<resultMap></resultMap>

 직원 출근시간 
<select id="" parameterType="" resultType="String">
 SELECT TO_CHAR(WORK_ON, 'yyyy/mm/dd hh24:mi') WORK_ON_TIME
   FROM ATTENDANCE
  WHERE  = #{dclz_seq, jdbcType=NUMERIC}
</select>

직원 퇴근시간
<select id="" parameterType="" resultType="String">
 SELECT TO_CHAR(WORK_OFF, 'yyyy/mm/dd hh24:mi') WORK_OFF_TIME
   FROM ATTENDANCE
  WHERE DCLZ_SEQ = #{dclz_seq, jdbcType=NUMERIC}
</select>

-->

</mapper>